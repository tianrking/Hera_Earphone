# 音频数据处理流程详解

## 概述
本文档详细说明了音频数据从原始PCM采集到通过BLE发送的完整处理流程。

## 1. 音频数据采集阶段

### 1.1 ADC采集 (位置: `cpu/br28/audio_enc.c`)
```c
static void adc_mic_output_handler(void *priv, s16 *data, int len)
```

**功能说明:**
- ADC（模数转换器）采集麦克风模拟信号
- 转换为16位PCM数字音频数据
- 数据格式：`s16*`（16位有符号整数数组）
- 采样率：16kHz（取决于配置）
- 通道数：单声道或双声道（根据`ESCO_ADC_CH`配置）

**关键参数:**
- `data`: 原始PCM数据指针
- `len`: 数据长度（字节数）
- 采样率：通常为8kHz或16kHz

### 1.2 数据缓冲 (位置: `cpu/br28/fft_and_pca.c`)
```c
void mic_insert_data(s16* data, int len)
```

**功能说明:**
- 将原始PCM数据存入环形缓冲队列
- 队列大小：`MIC_QUEUE_SIZE = 4000`个采样点
- 防止数据溢出处理

**代码流程:**
```c
// 1. 复制数据到临时缓冲区
memcpy(mic_temp_data, data, len * sizeof(s16));

// 2. 存入环形队列
for (int i = 0; i < len; i++) {
    mic_data_queue[mic_undone_offset] = mic_temp_data[i];
    mic_undone_offset = (mic_undone_offset + 1) % MIC_QUEUE_SIZE;
    mic_undone_count++;
}
```

## 2. Opus编码阶段

### 2.1 编码器初始化 (位置: `apps/common/third_party_profile/jieli/JL_rcsp/bt_trans_data/le_rcsp_adv_module.c`)
```c
// 打开麦克风Opus编码器
audio_mic_enc_open(rec_enc_mic_output, AUDIO_CODING_OPUS, 0 << 6);

// 打开解码器Opus编码器（用于参考音频）
audio_dec_enc_open(rec_enc_dec_output, AUDIO_CODING_OPUS, 0 << 6);
```

**编码参数:**
- `AUDIO_CODING_OPUS`: Opus编码格式
- `quality = 0`: 质量设置
- `sample_rate = 16000`: 16kHz采样率

### 2.2 编码回调函数
```c
// 麦克风编码输出回调
static int rec_enc_mic_output(void *priv, void *buf, int len)
{
    u8 *send_buf = (u8 *)buf;
    memcpy(opus_mic_buffer, send_buf, len);  // 保存压缩数据
    opus_mic_buffer_sent = false;             // 标记未发送
    return 0;
}

// 解码器编码输出回调
static int rec_enc_dec_output(void *priv, void *buf, int len)
{
    u8 *send_buf = (u8 *)buf;
    memcpy(opus_dec_buffer, send_buf, len);  // 保存压缩数据
    opus_dec_buffer_sent = false;             // 标记未发送
    return 0;
}
```

**关键变量:**
```c
// Opus压缩数据缓冲区 (40字节/通道)
u8 opus_mic_buffer[OPUS_PART_BYTE];    // 麦克风数据
u8 opus_dec_buffer[OPUS_PART_BYTE];    // 解码器数据

// 发送标志
bool opus_mic_buffer_sent;  // 麦克风数据是否已发送
bool opus_dec_buffer_sent;  // 解码器数据是否已发送

// 包大小定义
#define OPUS_PART_BYTE 40       // 单通道Opus数据大小
#define OPUS_PACKAGE_BYTE 84    // 完整音频包大小
```

## 3. 数据打包与发送

### 3.1 数据包结构
```
OPUS_PACKAGE_BYTE = 84字节
┌─────────────────────────────────────────────────────────┐
│ 字节 0      │ 字节 1-40           │ 字节 41-80           │
├─────────────┼─────────────────────┼─────────────────────┤
│ VAD状态     │ 麦克风Opus数据       │ 解码器Opus数据        │
│ (1字节)     │ (40字节)            │ (40字节)             │
└─────────────┴─────────────────────┴─────────────────────┘
```

### 3.2 打包发送逻辑 (原始代码)
```c
if (opus_mode) {
    // 清空数据包
    memset(opus_packages + opus_idx * OPUS_PACKAGE_BYTE, 0, OPUS_PACKAGE_BYTE);

    // 添加VAD状态
    opus_packages[opus_idx * OPUS_PACKAGE_BYTE] = vad_is_activate;

    // 添加麦克风Opus数据（40字节）
    if (!opus_mic_buffer_sent) {
        memcpy(opus_packages + opus_idx * OPUS_PACKAGE_BYTE + 1,
               opus_mic_buffer, OPUS_PART_BYTE);
    }

    // 添加解码器Opus数据（40字节）
    if (!opus_dec_buffer_sent) {
        memcpy(opus_packages + opus_idx * OPUS_PACKAGE_BYTE + 1 + OPUS_PART_BYTE,
               opus_dec_buffer, OPUS_PART_BYTE);
    }

    // 添加调试信息
    opus_packages[(opus_idx + 1) * OPUS_PACKAGE_BYTE - DEBUG_BYTE] = send_index;

    // 通过BLE发送
    int ret = app_send_user_data(
        ATT_CHARACTERISTIC_ae04_01_VALUE_HANDLE,
        opus_packages + opus_idx * OPUS_PACKAGE_BYTE,
        OPUS_PACKAGE_BYTE,
        ATT_OP_AUTO_READ_CCC
    );

    if (ret == 0) {
        opus_idx = (opus_idx + 1) % MAX_CONFLICT_COUNT;
        send_index++;
        failed_count = 0;
    }
}
```

## 4. BLE传输

### 4.1 发送接口
- **函数**: `app_send_user_data()`
- **特征值**: `ATT_CHARACTERISTIC_ae04_01_VALUE_HANDLE`
- **方式**: Notify（无需ACK）
- **MTU**: 最大247字节，有效载荷244字节

### 4.2 发送频率控制
```c
// 发送间隔控制
static u32 last_send_time = 0;
static u32 send_interval_ms = 5;  // 5ms间隔

// 时间判断
if (time_after(current_time, last_send_time + send_interval_ms)) {
    // 执行发送
    last_send_time = current_time;
}
```

## 5. 完整数据流

```
1. 麦克风 → ADC采集 → 原始PCM数据（16位，16kHz）
   ↓
2. mic_insert_data() → 存入环形缓冲队列
   ↓
3. Opus编码器 → 压缩为40字节/20ms
   ↓
4. rec_enc_mic_output() → 编码完成回调
   ↓
5. 数据打包 → 84字节包（VAD+双通道Opus）
   ↓
6. BLE Notify → 发送到接收端
```

## 6. 关键文件说明

| 文件路径 | 功能 | 关键函数/变量 |
|---------|------|--------------|
| `cpu/br28/audio_enc.c` | ADC音频采集 | `adc_mic_output_handler()`, `mic_insert_data()` |
| `cpu/br28/fft_and_pca.c` | 音频缓冲管理 | `mic_data_queue[]`, `mic_insert_data()` |
| `apps/common/third_party_profile/jieli/JL_rcsp/bt_trans_data/le_rcsp_adv_module.c` | Opus编码和BLE发送 | `audio_mic_enc_open()`, `test_data_send_packet()`, `opus_packages[]` |
| `cpu/br28/fft_and_pca.h` | 常量定义 | `OPUS_PART_BYTE`, `OPUS_PACKAGE_BYTE`, `PACKAGE_BYTE` |

## 7. 性能参数

### 7.1 音频参数
- **采样率**: 16kHz
- **位深**: 16位
- **编码**: Opus
- **压缩比**: 约40:1（原始1280字节 → Opus 40字节）

### 7.2 BLE传输参数
- **包大小**: 84字节
- **发送间隔**: 5ms
- **实际码率**: 84字节 × 200包/秒 = 16.8KB/s (134.4kbps)
- **延迟**: 约20-30ms

## 8. 注意事项

1. **缓冲区管理**: 需要防止音频数据溢出和丢失
2. **同步问题**: 麦克风和解码器数据需要同步发送
3. **发送频率**: 5ms间隔是最小间隔，可能导致BLE拥塞
4. **数据完整性**: Notify模式不保证100%送达率

---
*创建日期: 2025-12-10*