# Hera 耳机音频编码流程分析

## 概述
本文档分析Hera耳机的音频编码流程，重点分析麦克风数据的采集、处理和编码过程。

## 核心文件
- **主要文件**: [`cpu/br28/audio_enc.c`](cpu/br28/audio_enc.c)
- **FFT分析**: [`cpu/br28/fft_and_pca.c`](cpu/br28/fft_and_pca.c)

## 1. 音频编码初始化流程

### 1.1 入口函数
```c
int esco_enc_open(u32 coding_type, u8 frame_len)
```
- 位置: [`audio_enc.c:629`](cpu/br28/audio_enc.c#L629)
- 功能: 打开ESCO编码器
- 参数:
  - `coding_type`: 编码类型（MSBC/CVSD）
  - `frame_len`: 帧长度

### 1.2 编码器配置
- 采样率设置:
  - MSBC: 16kHz
  - CVSD: 8kHz
- 通道数: 1（单声道）

### 1.3 麦克风启动
根据不同的麦克风类型调用相应的启动函数：
- **PDM麦克风**: [`esco_pdm_mic_en()`](cpu/br28/audio_enc.c#L693)
- **IIS麦克风**: [`esco_iis_mic_en()`](cpu/br28/audio_enc.c#L700)
- **ADC麦克风**: [`esco_mic_en()`](cpu/br28/audio_enc.c#L717)

## 2. ADC麦克风数据处理流程（重点分析）

### 2.1 麦克风使能
```c
static int esco_mic_en(u8 en, u16 sr, u16 mic0_gain, ...)
```
- 位置: [`audio_enc.c:362`](cpu/br28/audio_enc.c#L362)
- 功能: 启用/禁用ADC麦克风

### 2.2 关键注册步骤
1. **分配句柄**:
   ```c
   esco_mic = zalloc(sizeof(struct esco_mic_hdl));
   ```

2. **配置麦克风通道**:
   - 根据配置打开1-4个麦克风通道
   - 设置各通道增益

3. **设置采样参数**:
   ```c
   audio_adc_mic_set_sample_rate(&esco_mic->mic_ch, sr);
   audio_adc_mic_set_buffs(&esco_mic->mic_ch, esco_adc_buf,
                          ESCO_ADC_IRQ_POINTS * 2, ESCO_ADC_BUF_NUM);
   ```

4. **注册输出回调函数** ⭐:
   ```c
   esco_mic->adc_output.handler = adc_mic_output_handler;  // 第415行
   audio_adc_add_output_handler(&adc_hdl, &esco_mic->adc_output);  // 第416行
   ```

5. **启动ADC采样**:
   ```c
   audio_adc_mic_start(&esco_mic->mic_ch);
   ```

### 2.3 数据处理回调
```c
static void adc_mic_output_handler(void *priv, s16 *data, int len)
```
- 位置: [`audio_enc.c:249`](cpu/br28/audio_enc.c#L249)
- 参数说明:
  - `data`: 原始麦克风数据（16位采样值）
  - `len`: 数据长度（字节数）

### 2.4 根据麦克风数量处理数据

#### 单麦克风模式（SingleMic）
```c
#if (ESCO_ADC_CH == 1)
    mic_insert_data(data, len >> 1);  // 调用FFT分析
    audio_aec_inbuf(data, len);       // 传入AEC算法
#endif
```
- 位置: [`audio_enc.c:355-357`](cpu/br28/audio_enc.c#L355)

#### 双麦克风模式（DualMic）
```c
#elif (ESCO_ADC_CH == 2)
    // 分离两个麦克风的数据
    s16 *mic0_data = data;
    s16 *mic1_data = esco_mic->tmp_buf;
    // 交错数据分离
    for (u16 i = 0; i < (len >> 1); i++) {
        mic0_data[i] = data[i * 2];
        mic1_data[i] = data[i * 2 + 1];
    }
    // 分别传入AEC算法
    audio_aec_inbuf_ref(data, len);        // 参考麦克风
    audio_aec_inbuf(mic1_data_pos, len);   // 主麦克风
#endif
```
- 位置: [`audio_enc.c:322-352`](cpu/br28/audio_enc.c#L322)

#### 三麦克风模式（TripleMic）
```c
#elif (ESCO_ADC_CH == 3)
    // 类似双麦克风，但需要分离3个通道
    s16 *mic_data[3];
    mic_data[0] = data;                      // MIC0
    mic_data[1] = data + (len / 2);          // MIC1
    mic_data[2] = data + (len / 2) * 2;      // MIC2
    // 数据重排后分别处理
#endif
```
- 位置: [`audio_enc.c:283-320`](cpu/br28/audio_enc.c#L283)

## 3. FFT和PCA分析

### 3.1 数据插入
```c
void mic_insert_data(s16* data, int len)
```
- 位置: [`fft_and_pca.c:71`](cpu/br28/fft_and_pca.c#L71)
- 功能: 将麦克风数据存入环形缓冲区供FFT分析

### 3.2 数据存储
```c
memcpy(mic_temp_data, data, len * sizeof(s16));
// 存入环形队列 mic_data_queue
for (int i = 0; i < len; i++) {
    mic_data_queue[mic_undone_offset] = mic_temp_data[i];
    mic_undone_offset = (mic_undone_offset + 1) % MIC_QUEUE_SIZE;
    mic_undone_count++;
}
```

## 4. 数据流向总结

```
麦克风硬件
    ↓
ADC采样
    ↓
adc_mic_output_handler (中断回调)
    ↓
┌─────────────┬──────────────┬──────────────┐
│  单MIC      │    双MIC     │    三MIC     │
│             │              │              │
│ mic_insert_ │ 数据分离      │ 数据分离      │
│ data()      │              │              │
│             │ audio_aec_   │ audio_aec_   │
│ audio_aec_  │ inbuf_ref()  │ inbuf_ref_1  │
│ inbuf()     │              │              │
│             │ audio_aec_   │ audio_aec_   │
│             │ inbuf()      │ inbuf()      │
└─────────────┴──────────────┴──────────────┘
    ↓
AEC（声学回声消除）算法
    ↓
PCM编码（MSBC/CVSD）
    ↓
蓝牙传输
```

## 5. 关键配置宏

```c
// 麦克风数量配置
#if TCFG_AUDIO_TRIPLE_MIC_ENABLE
    #define ESCO_ADC_CH 3    // 3麦克风
#elif TCFG_AUDIO_DUAL_MIC_ENABLE
    #define ESCO_ADC_CH 2    // 双麦克风
#else
    #define ESCO_ADC_CH 1    // 单麦克风
#endif

// 缓冲区配置
#define ESCO_ADC_BUF_NUM        3
#define ESCO_ADC_IRQ_POINTS     256
#define ESCO_ADC_BUFS_SIZE      (ESCO_ADC_BUF_NUM * ESCO_ADC_IRQ_POINTS * ESCO_ADC_CH)
```

## 6. 开发建议

1. **添加新的音频处理节点**:
   - 在 `adc_mic_output_handler` 中，AEC处理前后都可以添加自定义处理
   - 例如：降噪、增益控制、语音检测等

2. **修改FFT分析**:
   - 在 `mic_insert_data` 函数中可以添加自己的分析逻辑
   - 注意环形缓冲区的线程安全

3. **多麦克风配置**:
   - 通过修改配置宏选择麦克风数量
   - 注意不同模式下的数据排列方式不同

4. **性能优化**:
   - ADC中断回调中避免耗时操作
   - 考虑使用DMA传输减少CPU占用

## 7. 调试技巧

1. **开启调试输出**:
   ```c
   // 在适当位置添加调试信息
   printf("mic_data: %d, len: %d\n", data[0], len);
   ```

2. **数据导出**:
   - 可以将 `mic_data_queue` 中的数据导出到文件进行分析
   - 使用示波器或音频分析工具验证信号质量

3. **参数调整**:
   - 麦克风增益可通过 `audio_adc_mic_set_gain()` 调整
   - 采样率和缓冲区大小可根据实际需求调整

---

*更新日期: 2025-12-07*