# 噪声门限（Noise Gate）

## 功能介绍
衰减低于给定阈值的输入信号

## 使用说明
* **attackTime**:启动时间，当输入信号低于阈值时，噪声门限启动的时间。
* **releaseTime**:释放时间，当输入信号高于阈值，噪声门限恢复到原始信号的时间。
* **Threshold**: 噪声门限阈值。
* **gain**: 低于噪声门限阈值的增益。
* **bypass**：勾选后噪声门限不运行，运行内存也会释放
