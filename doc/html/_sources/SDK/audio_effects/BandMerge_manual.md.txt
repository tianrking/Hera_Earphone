# 频带合并（Band Merge）

## 功能介绍

频带合并（ 将多路数据相加）

## 参数介绍
*  **band0 gain**：频带0变化的增益，增加或减少多少dB
*  **band1 gain**：频带1变化的增益，增加或减少多少dB
*  **band2 gain**：频带2变化的增益，增加或减少多少dB
*  **bypass**：bypass使能后，只输出band0输入端口的数据
